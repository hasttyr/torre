// Where a user lands right after logging in or registering. Every role now
// has its own dashboard at the same path (its widgets are decided per role
// on the backend), so the landing spot no longer depends on the role.
export const HOME_PATH = "/panel";
